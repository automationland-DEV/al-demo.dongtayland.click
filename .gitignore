# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Cai dat Claude Code rieng cua tung may
.claude/settings.local.json

# Phu thuoc / build output (phong khi .gitignore con thieu)
node_modules/
